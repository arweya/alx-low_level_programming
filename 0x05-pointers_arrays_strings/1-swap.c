#include "main.h"

/**
 * swap_int -swaps value of two intergers
 * @:first interger
 * @b:second interger
 * Return: nothing
 */

void swap_int(int *a, int *b)
{
	int tmp = *a;

temp = *a;
*a = *b;
*b = temp;
}
